services:
  app:
    build:
      context: .
      target: dev
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    env_file:
      - .env
    command: air
